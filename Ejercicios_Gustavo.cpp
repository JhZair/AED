#include <iostream>
#include <vector>
using namespace std;

int Encontrar_Horizontal(int * a, vector<int> p){
	for(int * i = a;a + i < a + p.size();i++){			//como se puede iterar usando solo punteros
		if(*i == 
	}
}

int main(int argc, char *argv[]) {
	
	vector<int> vect(100);
	for(auto i : vect){
		if(i == 'P'){
			Encontrar_Horizontal(&i+1)
		}
	}
	
	
	return 0;
}

//encontrar ocurrencias de palabras patan, tapa, pato, tipa
// solo horizontal y verticalmente

// hacer programa que encuentre el camino mas corto en un laberinto
